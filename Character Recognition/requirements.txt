# =============================================================================
# AI Handwritten Character Recognition & OCR System
# Backend + ML Requirements
# Python >= 3.10 Support (Including macOS Apple Silicon, Windows & Linux)
# =============================================================================
# NOTE: Platform-dependent libraries (torch, torchvision, torchaudio) are NOT
# listed here. They are installed dynamically during automated setup (npm install)
# based on operating system, CPU architecture, and Python version.

# ---- Web Framework ----
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
httpx>=0.27.0
requests>=2.31.0

# ---- Data Validation & Settings ----
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ---- Database ----
sqlalchemy>=2.0.30
alembic>=1.13.1

# ---- Deep Learning & Vision Models ----
timm>=1.0.3

# ---- Image Processing & Scientific Computation ----
opencv-python-headless>=4.10.0
Pillow>=10.3.0
numpy>=1.26.0
scikit-image>=0.23.0
scikit-learn>=1.4.0
scipy>=1.13.0
matplotlib>=3.8.0

# ---- OCR Engines ----
easyocr>=1.7.1
pytesseract>=0.3.10
# NOTE: Tesseract native binary should be available on PATH for optimal multi-engine OCR:
#   macOS:   brew install tesseract
#   Ubuntu:  sudo apt install tesseract-ocr
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki

# ---- Document Export ----
reportlab>=4.2.0
python-docx>=1.1.2
fpdf2>=2.7.9

# ---- Utilities ----
python-dotenv>=1.0.1
loguru>=0.7.2
tqdm>=4.66.0
click>=8.1.7
rich>=13.7.1
tensorboard>=2.17.0

# ---- Testing & Quality Verification ----
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
ruff>=0.4.8
mypy>=1.10.0
